type Pet @model @auth(rules: [{allow: public, operations: [create, read, update]}, {allow: owner}]) {
  id: ID!
  name: String!
  age: Int
  breed: String
  about: String
  image: String
  color: String
}
 